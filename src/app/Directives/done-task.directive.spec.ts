import { DoneTaskDirective } from './done-task.directive';

describe('DoneTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DoneTaskDirective();
    expect(directive).toBeTruthy();
  });
});
